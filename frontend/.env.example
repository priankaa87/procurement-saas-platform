# The API gateway. Every request goes here; nothing talks to services directly.
VITE_API_BASE=http://localhost:8080

# Development only: a bearer token to send until Keycloak login is wired in.
# Leave empty to run signed-out. Never commit a real token.
VITE_DEV_TOKEN=
